[project]
name = "post-queue-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "ruff==0.15.4",
]

[tool.ruff]
line-length = 88
target-version = "py311"
